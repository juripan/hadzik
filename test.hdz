naj y = 1

// also chaining multiple comparisons leads to undefined behavior a.k.a javascript behavior
// because ((1 < 12) < 12) -> 1 < 12 -> 1

vychod(y >= 12)
//TODO: negative number don't work correctly for comparisons