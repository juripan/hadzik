program -> (stmt and \n)*
stmt -> {
    exit(expr)
    print(string or expr)
    return expr
}

expr -> bin_expr or factor

bin_expr -> {
factor (+ or -) factor prec = 0
factor (* or /) factor prec = 1
}

factor -> int or float or ident

ident -> int or float or ident